[tool.pytest.ini_options]
# Run the suite with a bare `pytest` from the project root: the console script, unlike
# `python -m pytest`, does not put the working directory on sys.path.
pythonpath = ["."]
testpaths = ["tests"]
