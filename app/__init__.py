"""Cleaning Robot API: the HTTP service and the domain it exposes.

Declaring the package explicitly keeps `app` a regular package instead of an implicit
namespace package, which is what packaging and type-checking tools expect.
"""
