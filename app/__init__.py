"""Cleaning Robot API: the HTTP service and the domain it exposes.

This file also marks the directory as a regular package rather than an implicit
namespace package: the application is a single self-contained unit, and packaging
and type-checking tools resolve it more predictably when that is stated explicitly.
"""
