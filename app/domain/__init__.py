"""Robot and map behaviour, free of any knowledge of HTTP or of the API contract.

The explicit declaration keeps the domain one closed package, so it can never be merged
with a same-named directory found elsewhere on the import path.
"""
